---
transition: fade
---

<script setup>
const contents = [
  {
    text: '<b>Q1:</b> Write a C program that reads an integer from the user. Using an <code>if</code> statement, check if the number is positive. If it is, print <code>"The number is positive"</code>. If not, print nothing and just exit.'
  },
  {
    text: '<b>Q2:</b> Write a C program that reads a student\'s mark (0–100). Using an <code>if</code> statement, check if the mark is greater than or equal to <code>50</code>. If true, print <code>"Pass"</code>. The program should do nothing if the condition is false.'
  }
]
</script>

<Slide
  topic="Decision Making in C"
  sub-topic="Practice Problems — Slide 2: The if Statement"
  :contents="contents"
/>
