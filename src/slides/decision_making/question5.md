---
transition: fade
---

<script setup>
const contents = [
  {
    text: '<b>Q1:</b> Write a C program that reads a student\'s mark and prints their grade using an <code>else if</code> ladder:<br>— <code>90–100</code>: <code>"Grade: S"</code><br>— <code>80–89</code>: <code>"Grade: A"</code><br>— <code>70–79</code>: <code>"Grade: B"</code><br>— <code>60–69</code>: <code>"Grade: C"</code><br>— <code>50–59</code>: <code>"Grade: D"</code><br>— Below <code>50</code>: <code>"Grade: F"</code>'
  },
  {
    text: '<b>Q2:</b> Write a C program that reads the current hour (0–23) in 24-hour format and uses an <code>else if</code> ladder to print the time of day:<br>— <code>5–11</code>: <code>"Good Morning"</code><br>— <code>12–16</code>: <code>"Good Afternoon"</code><br>— <code>17–20</code>: <code>"Good Evening"</code><br>— Any other value: <code>"Good Night"</code>'
  }
]
</script>

<Slide
  topic="Decision Making in C"
  sub-topic="Practice Problems — Slide 5: else if Ladder — Flow & Example"
  :contents="contents"
/>
