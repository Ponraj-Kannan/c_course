---
transition: fade
---

<script setup>
const contents = [
  {
    text: '<b>Q1:</b> Write a C program that declares an integer array of 7 elements. Use a <code>for</code> loop to traverse the array and print each element on a new line.'
  },
  {
    text: '<b>Q2:</b> Write a C program that reads 5 integers into an array from the user. Use a <code>for</code> loop to compute and print the <b>sum</b> and <b>average</b> of all elements.'
  },
  {
    text: '<b>Q3:</b> Write a C program that declares an integer array of 10 elements. Use a <code>for</code> loop to count and print how many elements are <b>even</b> and how many are <b>odd</b>.'
  }
]
</script>

<Slide
  topic="Arrays in C"
  sub-topic="Practice Problems — Traversing an Array"
  :contents="contents"
/>
